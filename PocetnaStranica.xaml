<Page x:Class="zavrsni_rad.PocetnaStranica"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:zavrsni_rad"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="500"
      Title="PocetnaStranica" Background="Cornsilk">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
            <Label Style="{StaticResource labelStyle1}" Foreground="Crimson" Margin="0 0 0 50" MouseLeftButtonUp="Label_MouseLeftButtonUp">Studenti</Label>
            <Label x:Name="strukt_pod" Style="{StaticResource labelStyle1}" Foreground="DarkGreen" MouseLeftButtonUp="Label_MouseLeftButtonUp_1">Strukture podataka</Label>
            <Label x:Name="prog_inz" Style="{StaticResource labelStyle1}" Foreground="DarkGreen" Margin="0 20" MouseLeftButtonUp="Label_MouseLeftButtonUp_2">Programsko inženjerstvo</Label>
        </StackPanel>
    </Grid>
</Page>
